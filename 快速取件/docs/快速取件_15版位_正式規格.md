# SPX 生成器－快速取件 15版位 正式規格

Status: Living Document（逐版位完成後持續更新） 建立日期：2026-09-14

## 1. 文件定位與治理

本文件是快速取件 01～15「逐版位 renderer／visual／encoder／verification」的
唯一集中式正式規格文件。與既有正式文件的治理關係：

1.  `SPX_快速取件_Phase0_完整需求整理.md`：保存需求探索與早期整理。
2.  `SPX_快速取件_Requirement_Specification_v1.0.md`：定義快速取件工具
    整體／共通正式需求。
3.  `SPX_快速取件_Proposal_v1.0.md`：定義共通平台與 implementation
    proposal。
4.  本文件：集中記錄 01～15 各版位最終正式 renderer、geometry、assets、
    typography、per-item differences、encoder、launcher／viewer（如適用）
    與 manual verification status。

共通規則（Workspace、Editor、banwords、Excel Import、KV 共通行為、
Color control、Logo mode contract、Preview／Export contract、Export
readiness gate 等）以既有正式文件為準，本文件不重複收錄；只有與「逐版位
實作」直接相關、或該版位有明確 override 時，才在本文件記錄。

治理規則（自本文件建立起生效）：

-   01～15 的逐版位正式規格統一維護於本文件。
-   後續每完成一個版位的流程：完成 renderer → self-test → Jamie manual
    PASS → Code Commit → 更新本文件該版位章節 → Docs Commit。
-   原則上不再為個別版位建立獨立 Requirement、Proposal、Renderer Spec 或
    Verification Spec，除非 Jamie 明確另行要求。
-   未完成版位章節不得預先填入任何推測規格（含依對位圖反推）。

## 2. 15 版位 Registry 總表

以下與 `快速取件/js/registry.js` 逐項核對一致（核對基準 commit：
`4aa2d7d5f1e5b8a01448600765dde6f52ad9354e`）。

| # | Name | Size | Format | Status |
|---|------|------|--------|--------|
| 01 | 01_DDcard BN | 531×792 | JPG | Implemented / Jamie Manual Verification PASS |
| 02 | 02_HBN | 1200×360 | JPG | Implemented / Jamie Manual Verification PASS |
| 03 | 03_LPBN | 1200×550 | JPG | Implemented / Jamie Manual Verification PASS |
| 04 | 04_POP UP | 580×720 | PNG | Implemented / Jamie Manual Verification PASS |
| 05 | 05_IG | 900×1600 | JPG | Implemented / Jamie Manual Verification PASS |
| 06 | 06_FB Post | 1200×630 | JPG | Implemented / Jamie Manual Verification PASS |
| 07 | 07_TVBN_有人店 | 1599×1080 | JPG | Implemented / Jamie Manual Verification PASS |
| 08 | 08_TVBN_智取店 | 1080×1920 | JPG | Implemented / Jamie Manual Verification PASS |
| 09 | 09_TVBN_旗艦店 | 1920×1080 | JPG | Implemented / Jamie Manual Verification PASS |
| 10 | 10_繳費機直式BN-立保 | 1080×1920 | JPG | Implemented（reuse 08 renderer）/ Jamie Manual Verification PASS |
| 11 | 11_繳費機直式BN-博辰 | 2700×3380 | JPG | Implemented / Jamie Manual Verification PASS |
| 12 | 12_繳費機下方BN-立保 | 1040×578 | JPG | Deferred / Not Implemented |
| 13 | 13_繳費機下方BN-博辰 | 984×309 | JPG | Deferred / Not Implemented |
| 14 | 14_AR | 100×100 | JPG | Deferred / Not Implemented |
| 15 | 15_MSBN | 1200×400 | JPG | Deferred / Not Implemented |

12～15 的 registry 條目、對位圖或共通 Workspace 完成，均不代表該版位
renderer 已完成。

## 3. 01_DDcard BN 正式規格

### 3.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
-   Code Commit：`4aa2d7d5f1e5b8a01448600765dde6f52ad9354e`
    （`feat(quick-pickup): add DDcard BN renderer`）
-   rendererKey：`01-ddcard-bn`
-   Renderer module：`快速取件/js/renderers/01-ddcard-bn.js`，經
    `快速取件/js/renderers/index.js`（renderer manifest）以
    `registerRenderer()` 註冊。

### 3.2 Canvas 與背景

-   Canvas：531 × 792。
-   Export format：JPG。
-   無固定背景圖。整張 canvas 先以 `state.shared.colors.background`
    填滿（`fillRect(0, 0, 531, 792)`）。

### 3.3 Draw Order

正式 renderer 依序繪製（`globalAlpha = 1`、`source-over`）：

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標）
6.  CTA

Alignment overlay 不是正式 renderer layer（見 3.13）。

### 3.4 Logo

-   正式 runtime assets：`快速取件/assets/店到店_橘.png`、
    `快速取件/assets/店到店_白.png`。
-   Logo box：x = 90、y = 103、width = 351、height = 50。
-   規則：preserve aspect ratio、contain
    （`scale = min(boxW/srcW, boxH/srcH)`）、no crop、no stretch、
    水平置中、垂直置中、destination coordinates 不強制 round、
    high-quality image smoothing。
-   Logo mode：auto｜orange｜white。auto 使用共通正式
    `resolveLogoVariant()`（threshold 0.498708；背景 relative luminance
    >= threshold → orange，< threshold → white）。
-   Workspace 只保存 mode，不保存 computed variant；variant 一律由
    renderer 於 render 時解算。
-   Logo asset 載入失敗即 render fail-closed，不 fallback、不省略。

### 3.5 主標

-   box：x = 90、y = 170、w = 351、h = 37。
-   font：`30pt "ShopeeNotoSans Medium"`。
-   text：`state.shared.text.title`；color：`state.shared.colors.title`。
-   alignment：ink-box centering（actualBoundingBox 量測置中）。
-   空字串：skip drawing。

### 3.6 副標

-   box：x = 43、y = 221、w = 445、h = 57。
-   font：`45pt "ShopeeNotoSans Bold"`。
-   text：`state.shared.text.subtitle`；color：
    `state.shared.colors.subtitle`。
-   alignment：整串單一 font 的 ink-box centering。
-   空字串：skip drawing。

正式裁決（Jamie Phase E 最終人工驗證後裁定）：快速取件 01 不沿用
FSS D－01 的 $／% 縮小規則。副標所有字元（含 `$`、`%`）一律
`45pt "ShopeeNotoSans Bold"`，無 symbol font、無特殊縮小、無 mixed-font
subtitle、無特殊 symbol baseline alignment。

### 3.7 小字

-   box：x = 43、y = 296、w = 445、h = 22。
-   font：`18pt "ShopeeNotoSans Medium"`。
-   text：`state.shared.text.small`；color：`state.shared.colors.small`。
-   alignment：ink-box centering。
-   空字串：skip drawing。

### 3.8 Font Rendering

-   pt 規則：字級直接以 pt font string 交給 Canvas，禁止人工
    pt × 96 / 72 或其他換算。
-   字型：renderer module 內以 FontFace API 建立
    `ShopeeNotoSans Medium`／`ShopeeNotoSans Bold` 別名，指向 SPX 既有
    正式 WOFF2；具 `document.fonts.load()` ＋ `document.fonts.check()`
    readiness gate，字型未就緒即 fail-closed，不得默默 fallback 系統字型。
    不修改共通 `快速取件/css/styles.css`。
-   Medium rendering：主標與小字採 renderer-local 2× supersampling —
    temp canvas 1062 × 1584、`scale(2, 2)`、以原始 531×792 geometry
    繪製，再 high-quality downsample 回正式 canvas。
-   副標 Bold 不走 Medium 2× layer，直接於正式 canvas 繪製。
-   以上為 01 目前實際 implementation 之記錄，不得推廣為其他版位的
    必然規則。

### 3.9 KV

-   來源：`state.shared.kv`（使用者上傳，共通 KV contract）。
-   box：x = 25、y = 329、w = 482、h = 333。
-   規則：preserve source aspect ratio、contain、no crop、no stretch、
    水平置中、垂直置中、destination coordinates 不強制 round。
-   `state.shared.kv === null`：不畫 placeholder，該區直接保留
    background；不阻擋 Preview。

### 3.10 CTA

-   asset：`快速取件/assets/看更多CTA.png`（原始尺寸 139 × 44）。
-   geometry：x = 196、y = 675、w = 139、h = 44。
-   正式顯示：1:1 原尺寸繪製，無縮放。
-   Renderer 具 intrinsic size fail-closed validation：CTA asset 原始
    尺寸非 139 × 44 時明確 throw，不縮放、不重畫。
-   asset 載入失敗即 render fail-closed。

### 3.11 Assets 分類

01 Code Commit（`4aa2d7d`）納入四份 assets，分類如下：

正式 renderer runtime dependency：

-   `快速取件/assets/店到店_橘.png`
-   `快速取件/assets/店到店_白.png`
-   `快速取件/assets/看更多CTA.png`

Viewer verification dependency（非 renderer／Export layer）：

-   `快速取件/assets/對位/01_DDcard BN.png`（531 × 792）

01 對位圖不是 background、不是 renderer layer、不是 Export layer，
只作為 Viewer 人工驗證 overlay（見 3.13）。

尚未納入 repo tracking 的 `assets/對位/12～13` 對位圖
均不是 01 dependency。

### 3.12 Preview／Export Renderer Contract

-   Preview 與 Export 共用正式 `renderItemToCanvas()`；01 renderer 經
    renderer manifest 註冊為 `01-ddcard-bn`，無 Preview-only renderer。
-   Viewer verification controls 不影響正式 Export。

### 3.13 Viewer／Launcher 與人工驗證模式

-   Viewer：`快速取件/launch/viewer.html?item=01`。
-   Launcher：`快速取件/launch/01_DDcard BN.command`（port 4175）。
    Finder 雙擊 → 啟動或安全重用正確 local server（以 Viewer marker
    驗證）→ Google Chrome 開啟 01 Viewer。port 4175 被非預期 process
    佔用時 fail-closed 明確報錯，不默默開到錯誤 server。不依賴 FSS
    runtime。
-   Viewer 內含 development-only「人工驗證模式」（僅已接入驗證的版位
    顯示，目前為 01～11）：
    temporary test title／subtitle／small text、KV upload、KV remove、
    background color、title／subtitle／small color、Logo
    auto/orange/white、alignment overlay toggle、overlay opacity。
-   這不是正式 Workspace UI。verification state 只存在 Viewer page
    memory，不寫入 Workspace、Workspace JSON、localStorage、
    sessionStorage、indexedDB、Repo 或 assets。
-   Viewer KV upload 重用正式 `js/kv.js` `fileToKv()`，Viewer 不建立
    另一套 decode／validation。
-   Alignment overlay：asset 為 `assets/對位/01_DDcard BN.png`，
    viewer-only、absolute overlay、`pointer-events: none`、opacity 可調、
    不修改 canvas pixels、不進 renderer、不進 Export；renderer 重新
    render 後 overlay 重新掛在最新 canvas 上並精確重疊。

### 3.14 JPG Encoder

registry `encoderOptions`：

-   quality：1.0
-   maxBytes：245000
-   qualityFloor：0.5
-   searchSteps：7

正式流程：

1.  以 quality 1.0 encode。
2.  blob.size <= 245000 bytes → 直接使用。
3.  超限 → 以 qualityFloor 0.5 encode。
4.  floor 仍超限 → fail-closed（整次 Export 失敗）。
5.  否則 binary search 7 steps。
6.  取符合 maxBytes 的最高 quality。

此策略結構性搬用 FSS BN 現行 JPG compression mechanism（Jamie 裁決）。
快速取件 01 沒有 72 DPI metadata requirement，encoderOptions 不含 dpi，
不做 DPI byte patch；正式輸出規格以 531 × 792 pixels 為準。

### 3.15 Readiness

-   01 renderer：ready。
-   01 JPG encoder policy：ready。
-   整體 15 項「下載完整專案」Export 仍 fail-closed：12～15 尚未完成
    正式 renderer／encoder readiness。01～11 完成不代表目前可正式輸出
    完整 15 項專案。

### 3.16 Verification Record

-   Phase A：Repository Investigation completed。
-   Phase B：Proposal / implementation plan approved。
-   Phase C：Coding completed。
-   Phase D：AI/Codex self-test completed。
-   Phase E：Jamie manual verification PASS。

Jamie 最終人工驗證涵蓋：Logo positioning／scaling、Logo
auto/orange/white、深淺背景 auto 自動切換、title、subtitle、small text、
text colors、background color、KV upload/remove、KV contain／center、
CTA、alignment overlay。

最終裁決記錄：副標 $／% 不縮小，全部使用 45pt Bold（見 3.6）。

## 4. 02_HBN 正式規格

### 4.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
-   Code Commit：`574e98d74ac1414acdbcaa62229ad7df85f56eed`
    （`feat(quick-pickup): add HBN renderer`）
-   rendererKey：`02-hbn`
-   Renderer module：`快速取件/js/renderers/02-hbn.js`（依 Jamie 裁決
    自含必要 helper，不修改、不 import 01 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/02_HBN.command`；Viewer：
    `快速取件/launch/viewer.html?item=02`；Alignment：
    `快速取件/assets/對位/02_HBN.png`。

### 4.2 Canvas 與背景

-   Canvas：1200 × 360。Export format：JPG。
-   無固定背景圖（不得記為 FSS D－02 fixed background）。整張 canvas
    以 `state.shared.colors.background` 填滿。
-   Alignment image 只供 Viewer 人工對位驗證，不進 renderer／Preview／
    Export layer。

### 4.3 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標）
6.  CTA

（`globalAlpha = 1`、`source-over`；alignment overlay 不屬 renderer
draw order。）

### 4.4 Logo

-   正式 runtime assets：`快速取件/assets/店到店_橘.png`、
    `快速取件/assets/店到店_白.png`（01 已 tracked，直接重用）。
-   Logo box：x = 98、y = 96、w = 351、h = 50。
-   規則：contain、preserve aspect ratio、no crop、no stretch、
    high-quality smoothing、destination coordinates 不強制 round。
-   02 特有（依 FSS D－02）：**LEFT + TOP** —
    `destinationX = box.left`、`destinationY = box.top`，
    不是 01 的水平置中。
-   Logo mode：auto｜orange｜white，重用共通 `resolveLogoVariant()`
    （threshold 0.498708；L >= threshold → orange，L < threshold →
    white）。asset 載入失敗 fail-closed。

### 4.5 主標

-   box：(98, 153, 351×37)；font：`30pt "ShopeeNotoSans Medium"`
    （pt 直接使用，不得人工換算 px）。
-   source：`state.shared.text.title`；color：
    `state.shared.colors.title`。
-   alignment：ink-box **LEFT + TOP**（`x = box.left − inkLeft`、
    `y = box.top − inkTop`）。空字串 skip。

### 4.6 副標

-   box：(98, 200, 445×57)；font：`45pt "ShopeeNotoSans Bold"`。
-   source：`state.shared.text.subtitle`；color：
    `state.shared.colors.subtitle`。
-   alignment：整串 ink-box **LEFT + TOP**。空字串 skip。
-   快速取件正式 override：所有字元（含 `$`、`%`）一律
    `45pt "ShopeeNotoSans Bold"`；02 不沿用 FSS D－02 的 37pt symbol
    font、mixed-font subtitle、symbol baseline alignment 或 $/%
    特殊縮小。

### 4.7 小字

-   box：(98, 273, 445×22)；font：`18pt "ShopeeNotoSans Medium"`。
-   source：`state.shared.text.small`；color：
    `state.shared.colors.small`。
-   alignment：ink-box **LEFT + TOP**。空字串 skip。

### 4.8 Medium local 2×

-   主標＋小字走 renderer-local 2× supersampling：temp canvas
    **2400 × 720**、`scale(2, 2)`、仍以原始 1200×360 geometry 繪製，
    完成後 high-quality downsample 回 1200×360。
-   `title === "" && small === ""` 時 early-return（沿 01，不沿 FSS
    D－02 的空 canvas 行為）。
-   副標 Bold 不走 Medium layer。

### 4.9 KV

-   source：`state.shared.kv`；box：x = 566、y = 20、w = 460、h = 319。
-   規則：contain、preserve aspect ratio、no crop、no stretch、水平＋
    垂直置中、destination coordinates 不強制 round、high-quality
    smoothing。
-   KV null：skip、保留 background、不畫 placeholder。
-   座標溯源：原設計畫板 CSS global left 1179，經 1200×360 alignment
    image 與 CTA 交叉驗證出 artboard offset 613，故正式 canvas-local
    x = 566。global coordinate 不是 renderer 正式 geometry。

### 4.10 CTA

-   asset：`快速取件/assets/看更多CTA.png`（source intrinsic 139 × 44，
    renderer 驗證不符即 fail-closed）。
-   02 正式 destination：x = 1038、y = 290、w = 140、h = 44，貼左。
-   02 per-item override（Jamie 裁決）：source 139×44 → destination
    140×44，允許 1px 水平 scaling。不是 01 的 1:1 規則；不得記為
    139×44 destination、1038.5 或水平置中。
-   座標溯源：原 CSS global left 1651 − artboard offset 613 = 1038。

### 4.11 Font／Fail-Closed

-   renderer-local FontFace aliases（`ShopeeNotoSans Medium`／
    `ShopeeNotoSans Bold`，SPX 既有正式 WOFF2）；需要 30pt Medium、
    45pt Bold、18pt Medium 三個 font 字串（無 37pt requirement）。
-   font load/check 失敗、Logo/CTA 載入失敗、CTA intrinsic 非 139×44、
    KV decode 失敗，一律 fail-closed，不 silent fallback。

### 4.12 JPG Encoder

-   registry `encoderOptions`：quality 1.0、maxBytes 145000、
    qualityFloor 0.5、searchSteps 7。
-   沿現有 generic `encodeJpegWithinLimit`（export.js 零修改）：
    quality 1.0 encode → ≤ 145,000 bytes 直接採用 → 否則 floor 0.5、
    仍超限 fail-closed → 否則 7 步 binary search 取符合上限的最高
    quality。
-   02 無 72 DPI metadata patch、無 DPI requirement。

### 4.13 Viewer 人工驗證

-   02 已接入既有人工驗證模式（無第二套 UI）：verification test
    文字為主標「春季加碼抽」（5）、副標「副標$7個字」（5）、小字
    「到貨7天內完成取貨即可參加」（12.5），weighted lengths 均合法。
-   02 可驗證：KV upload/remove、background color、title/subtitle/
    small color、Logo auto/orange/white、alignment overlay、overlay
    opacity。verification state 僅存 page memory，不進 Workspace／
    JSON／localStorage／Export。
-   Jamie 已以此模式完成 Manual Verification = PASS。

### 4.14 Launcher

-   `快速取件/launch/02_HBN.command`（executable）：port 4175、
    target `viewer.html?item=02`、沿 01 launcher 已驗證 pattern —
    可安全重用正確 4175 server（以 Viewer marker 驗證）、錯誤 process
    佔用 fail-closed、Google Chrome 開啟 Viewer。

### 4.15 Verification Record

-   Phase A：Repository Investigation completed。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Codex self-test completed。
-   Phase E：Jamie manual verification PASS。

## 5. 03_LPBN 正式規格

### 5.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
-   Code Commit：`3c118c879ebe10fe40573c654dede24826a9cfba`
    （`feat(quick-pickup): add LPBN renderer`）
-   rendererKey：`03-lpbn`
-   Renderer module：`快速取件/js/renderers/03-lpbn.js`（依 Jamie 裁決
    自含必要 helper，不修改、不 import 01／02 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/03_LPBN.command`；Viewer：
    `快速取件/launch/viewer.html?item=03`；Alignment：
    `快速取件/assets/對位/03_LPBN.png`（1200×550 RGBA）。

### 5.2 FSS Reference

快速取件與 FSS 的版位編號不同：03_LPBN 的 FSS 唯讀參考來源為
**FSS D－12（12_LPBN，1200×550）**，不是 FSS D－03（Coin page BN，
1200×391）。FSS 僅為 reference，快速取件 03 不等於 FSS placement
number 03；下列 Jamie overrides 優先於 FSS。

### 5.3 Canvas 與背景

-   Canvas：1200 × 550。Export format：JPG。controlsProfile：
    shared-01-13。
-   無固定背景圖。整張 canvas 以 `state.shared.colors.background`
    填滿。
-   Alignment image 只供 Viewer 人工對位驗證，不進 renderer／Preview／
    Export layer。

### 5.4 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標）

03 沒有 CTA layer（見 5.10）。（`globalAlpha = 1`、`source-over`；
alignment overlay 不屬 renderer draw order。）

### 5.5 Logo

-   正式 runtime assets：`快速取件/assets/店到店_橘.png`、
    `快速取件/assets/店到店_白.png`（已 tracked，直接重用）。
-   Logo box：x = 58、y = 161、w = 365、h = 52。
-   正式縮放：`scale = min(box.width / source.width,
    box.height / source.height)` — contain、preserve ratio、no crop、
    no stretch、destination coordinates 不強制 round、high-quality
    smoothing。
-   Jamie override：03 不採 FSS D－12 的 `min(1, …)` source upscale
    clamp（無放大上限條款）。
-   alignment（03 專屬）：**horizontal LEFT ＋ vertical CENTER** —
    `destX = box.left`、`destY = box.top + (box.height − destH) / 2`。
    03 不是 01 的水平置中，也不是 02 的垂直貼上。
-   目前正式 1401×275 Logo → 約 264.916…×52 @ (58, 161)。
-   variant 重用共通 `resolveLogoVariant()`（auto｜orange｜white，
    threshold 0.498708）。asset 載入失敗 fail-closed。

### 5.6 主標

-   box：(58, 226, 405×49)；font：`39pt "ShopeeNotoSans Medium"`
    （pt 原樣，不得人工換算 px）。
-   source：`state.shared.text.title`；color：
    `state.shared.colors.title`。
-   alignment：ink-box **LEFT ＋ vertical CENTER** —
    `x = box.left − inkLeft`、
    `y = box.top + box.height / 2 − (inkTop + inkBottom) / 2`。
-   空字串 skip。

### 5.7 副標

-   box：(58, 285, 475×62)；font：`49pt "ShopeeNotoSans Bold"`。
-   source：`state.shared.text.subtitle`；color：
    `state.shared.colors.subtitle`。
-   alignment：整串 single-font ink-box **LEFT ＋ vertical CENTER**。
    空字串 skip。
-   Jamie 正式裁決：所有字元（含 `$`、`%`）一律
    `49pt "ShopeeNotoSans Bold"`；03 明確不搬 FSS D－12 的 42pt
    symbol font、mixed-font subtitle、symbol baseline 特殊處理與
    $/% 縮小——這些 FSS 行為不是快速取件正式規格。

### 5.8 小字

-   box：(58, 360, 475×28)；font：`22.5pt "ShopeeNotoSans Medium"`
    （22.5pt 精確保留，不取整、不換算）。
-   source：`state.shared.text.small`；color：
    `state.shared.colors.small`。
-   alignment：ink-box **LEFT ＋ vertical CENTER**。空字串 skip。

### 5.9 Medium local 2× 與 KV

-   主標＋小字走 renderer-local 2× supersampling：temp canvas
    **2400 × 1100**、`scale(2, 2)`、仍以原始 1200×550 geometry 繪製，
    high-quality downsample 回 1200×550。`title === "" &&
    small === ""` 時 early-return。副標 Bold 不走 Medium layer。
-   KV：source `state.shared.kv`；box：x = 547、y = 61、w = 625、
    h = 432。contain、preserve ratio、水平＋垂直置中、no crop、
    no stretch、destination 不強制 round、high-quality smoothing。
    KV null → skip、保留 background、無 placeholder。
-   KV 座標溯源：Jamie 原始 CSS global (left 1160, top 490, 625×432)；
    alignment image 實測 local (547, 61, 625×432)；artboard offset
    X = 613、Y = 429。正式 renderer 使用 local 座標，不使用 global。

### 5.10 CTA

**03 沒有 CTA。** renderer 不載入 CTA asset（含 `看更多CTA.png`）、
不建立 CTA geometry、不繪製 CTA、不做 CTA 驗證。01／02 有 CTA
不代表 03 有 CTA。

### 5.11 Font／Fail-Closed

-   renderer-local FontFace aliases（`ShopeeNotoSans Medium`／
    `ShopeeNotoSans Bold`，SPX 既有正式 WOFF2）。
-   正式 font requirements：39pt Medium、49pt Bold、22.5pt Medium；
    **無 42pt symbol requirement**。
-   font load/check 失敗、Logo 載入失敗、KV decode 失敗，一律
    fail-closed，不 silent fallback。

### 5.12 JPG Encoder

-   registry `encoderOptions`：`Object.freeze({ quality: 1.0 })` —
    quality-only（依 FSS LPBN 現行機制）。
-   正式沒有 maxBytes、qualityFloor、searchSteps、DPI requirement；
    無 binary-search compression gate、無 72 DPI metadata patch。
-   不得把 01（245,000）／02（145,000）的 byte limit 套到 03。

### 5.13 Viewer 人工驗證

-   03 已接入既有人工驗證模式（無第二套 UI）：verification text 為
    主標「春季加碼抽」（weighted 5）、副標「副標最多$7個字」
    （weighted 7.0，滿限且含 $ 以驗證不縮小）、小字
    「到貨1天內完成取貨即可參加」（weighted 12.5），均合法。
-   可驗證：KV upload/remove、background color、title/subtitle/small
    color、Logo auto/orange/white、alignment overlay、overlay
    opacity。verification state 僅存 page memory，不進 Workspace／
    JSON／localStorage／Export。
-   Jamie Phase E：PASS。

### 5.14 Verification Record

-   Phase A：Repository Investigation completed。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Codex self-test completed。
-   Phase E：Jamie manual verification PASS。

## 6. 04_POP UP 正式規格

### 6.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
-   Code Commit：`8c5dccb3cf8d8cfee260880da358179b50975f41`
    （`feat(quick-pickup): add POP UP renderer`）
-   rendererKey：`04-pop-up`
-   Renderer module：`快速取件/js/renderers/04-pop-up.js`（自含必要
    helper，不修改、不 import 01～03 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/04_POP UP.command`；Viewer：
    `快速取件/launch/viewer.html?item=04`；Alignment：
    `快速取件/assets/對位/04_POP UP.png`（580×720 RGBA，MD5
    `ab7cdef0fc2442e6fa8803b791274958`）。
-   04 為快速取件第一個正式 PNG 版位。

### 6.2 FSS Reference

快速取件與 FSS 的版位編號不同：04_POP UP 的 FSS 唯讀參考來源為
**FSS BN D－10（10_POP UP，580×720，PNG）**；快速取件 04 不等於 FSS
placement number 04。FSS D－10 僅作 geometry／typography／PNG encoder
behavior 的唯讀 reference，下列 Jamie overrides 優先於 FSS。

### 6.3 Canvas 與背景（透明＋圓角卡片）

-   Canvas：580 × 720。Export format：PNG。controlsProfile：
    shared-01-13。
-   Canvas 外圍初始透明；**不做 full-canvas background fill**。
-   Workspace 背景色只填 rounded background card：x = 53、y = 27、
    w = 475、h = 658、**radius = 40px**（以 moveTo／arcTo path 建立，
    只 fill 卡片區域）。卡片外一律保持 transparent（alpha = 0）。
-   正式卡片高度為 **658**（Jamie CSS＋對位圖），不是 FSS D－10
    background asset 的 673。
-   座標溯源：畫板 global (1948, 27) 經 artboard offset X = 1895／
    Y = 0 換算為 local (53, 27)；KV／CTA 同 offset 交叉驗證一致。

### 6.4 Draw Order

1.  Transparent canvas（不填色）
2.  Rounded background card（radius 40，fill Workspace 背景色）
3.  KV（`state.shared.kv` 存在時）
4.  Logo
5.  Medium text layer（主標＋小字，local 2× supersampling）
6.  Bold subtitle（副標）
7.  CTA

不得把整個 renderer clip 到 rounded card（CTA 跨出卡片下緣為正式
設計）。Alignment overlay 只供 Viewer 人工驗證，不進 renderer／export。

### 6.5 Logo

-   正式 runtime assets：`快速取件/assets/店到店_橘.png`、
    `快速取件/assets/店到店_白.png`（已 tracked，直接重用）。
-   Logo box：x = 129、y = 109、w = 323、h = 46。
-   Logo mode：auto｜orange｜white；auto 沿共用
    `resolveLogoVariant()`（WCAG sRGB relative luminance，threshold
    0.498708）。
-   drawing：contain（`scale = min(boxW/srcW, boxH/srcH)`，不使用
    min(1, …) clamp）、preserve ratio、水平置中＋垂直置中、no crop、
    no stretch、destination 不強制 round、high-quality smoothing。
-   asset 載入／decode 失敗 fail-closed。

### 6.6 Typography

-   主標：box (129, 172, 323×38)、`30pt "ShopeeNotoSans Medium"`、
    Workspace title color、ink-box 水平＋垂直置中、空字串 skip。
-   副標：box (85, 225, 410×51)、`40pt "ShopeeNotoSans Bold"`、
    Workspace subtitle color、整串 single-font ink-box 水平＋垂直
    置中、空字串 skip。**所有字元（含 `$`、`%`）一律 40pt Bold**；
    04 不沿用 FSS D－10 的 35pt symbol font、symbol 縮小、mixed-font
    或特殊 baseline 處理——這些不是快速取件正式規格。
-   小字：box (85, 286, 410×25)、`20pt "ShopeeNotoSans Medium"`、
    Workspace small color、ink-box 水平＋垂直置中、空字串 skip。
-   所有 pt 直接保持 pt，不得人工換算 px。

### 6.7 Medium Rendering

-   主標＋小字走 renderer-local 2× supersampling：temp canvas
    **1160 × 1440**、`scale(2, 2)`、仍以原始 580×720 geometry 繪製，
    high-quality downsample 回 580×720。
-   `title === "" && small === ""` 時 early-return。
-   副標 Bold 直接繪於正式 canvas，不走 Medium layer。

### 6.8 KV

-   source：`state.shared.kv`；box：x = 69、y = 323、w = 442、
    h = 305（global (1964, 323) − offset 1895/0）。
-   contain、preserve ratio、水平＋垂直置中、no crop、no stretch、
    destination 不強制 round、high-quality smoothing。
-   KV null → skip、無 placeholder；decode 失敗 fail-closed。

### 6.9 CTA

-   正式 asset：`快速取件/assets/逛逛去CTA.png`（intrinsic 226 × 60
    RGBA，MD5 `efd8e080ff9f9d885e25ce5f0733f725`）。
-   destination：x = 177、y = 640、w = 226、h = 60（global
    (2072, 640) − offset 1895/0）。**1:1 繪製、無縮放**。
-   intrinsic 非 226×60 → fail-closed。
-   CTA 跨出 rounded background card 下緣為正式設計，不得 clip 到
    card bounds。

### 6.10 PNG Encoder

registry `encoderOptions`：`Object.freeze({ maxBytes: 250000, dpi: 72,
indexedFallbackColors: 256 })`。正式流程（完整比照 FSS BN D－10）：

1.  native `canvas.toBlob("image/png")`
2.  patch PNG pHYs = 72 DPI（**2835 pixels per meter，X = 2835、
    Y = 2835、unit = meter**；byte-level chunk，含正確 CRC32，不動
    IDAT pixels）
3.  patched native PNG ≤ 250,000 bytes → 採用
4.  超限 → 需要 `pako`＋`UPNG` globals，缺少 → fail-closed
5.  由**原始 canvas raw RGBA**（getImageData）`UPNG.encode(…, 256)`
    產生 256 色 indexed fallback（保留 alpha；不以 patched PNG 為
    pixel 來源）
6.  fallback 再 patch pHYs 72 DPI
7.  ≤ 250,000 bytes → 採用；仍超限 → throw，整次 Export fail-closed

PNG 無 quality／qualityFloor／searchSteps／binary search（不得與 JPG
quality search 混淆）。JPG 01～03 encoder 路徑零行為改變。

### 6.11 Vendor（Export encoder dependency）

-   Quick Pickup 自有 vendor：`快速取件/js/vendor/upng.js`、
    `pako.min.js`、`LICENSE.upng.txt`、`LICENSE.pako.txt`——由 FSS
    正式 vendor **byte-for-byte 複製**（SHA-256 一致）。無 CDN、無
    FSS runtime dependency。
-   載入順序（`快速取件/index.html`）：pako → UPNG → module app.js。
-   Vendor 是 **Export encoder dependency**，不是 04 renderer
    dependency；Viewer 不需要也不載入 encoder vendor。
-   Implementation note：`upng.js` 為 byte-for-byte vendored
    third-party source，保留 upstream／FSS 原始 whitespace（Jamie
    裁決之 vendor-only exception，僅適用此檔，不適用任何自有程式碼）。

### 6.12 Fail-Closed

Font load 失敗、Logo load/decode 失敗、KV decode 失敗、CTA load 失敗、
CTA intrinsic 不符、PNG fallback 需要但 pako/UPNG 未載入、fallback PNG
仍 > 250,000 bytes——一律 fail-closed，不得 silent fallback。

### 6.13 Viewer 人工驗證

-   04 已接入既有人工驗證模式（無第二套 UI）：verification text 為
    主標「春季加碼抽」（weighted 5）、副標「副標最多$7個字」
    （weighted 7.0，含 $ 驗證不縮小）、小字
    「到貨1天內完成取貨即可參加」（weighted 12.5）。
-   overlay：`../assets/對位/04_POP UP.png`。可驗證 rounded
    background、卡片外透明、背景色、Logo 三模式、三文字、KV、CTA、
    alignment overlay。verification state 僅存 page memory。
-   Jamie Phase E：PASS。

### 6.14 Verification Record

-   Phase A：Repository Investigation completed。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Codex self-test completed（含 pHYs byte-level 與
    UPNG fallback 邏輯層單元驗證）。
-   Phase E：Jamie manual verification PASS。

## 7. 05_IG 正式規格

### 7.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
-   Code Commit：`0a40c0f3f983b497a7fb1038d3249b38a472e535`
    （`feat(quick-pickup): add IG renderer`）
-   rendererKey：`05-ig`
-   Renderer module：`快速取件/js/renderers/05-ig.js`（自含必要
    helper，不修改、不 import 01～04 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/05_IG.command`（100755）；Viewer：
    `快速取件/launch/viewer.html?item=05`；Alignment：
    `快速取件/assets/對位/05_IG.png`（900×1600 RGBA，MD5
    `47695934430db03a8c2b6be85ba1f260`；正式實際路徑為
    `assets/對位/`，不是 `assets/C/對位/`）。

### 7.2 FSS Reference

快速取件與 FSS 的版位編號不同：05_IG 的 FSS 唯讀參考來源為
**FSS BN D－06（06_IG，900×1600，JPG）**；快速取件 05 不等於 FSS
placement number 05。FSS D－06 僅為 geometry／typography／encoder
behavior 的唯讀 evidence，下列 Jamie overrides 優先於 FSS。

### 7.3 Canvas 與背景

-   Canvas：900 × 1600。Export format：JPG。controlsProfile：
    shared-01-13。
-   無固定底圖。整張 canvas 以 `state.shared.colors.background`
    填滿（`fillRect(0, 0, 900, 1600)`）。非 transparent canvas、
    非 rounded card。

### 7.4 Group Shift（正式 geometry contract）

Logo／Title／Subtitle／Small 相對 FSS D－06 整組位移：
**ΔX = 0、ΔY = +96、w/h 不變**。

| 元素 | FSS D－06 | Quick Pickup 05 | ΔY |
|---|---|---|---|
| Logo | 161,282,580×82 | **161,378,580×82** | +96 |
| Title | 175,387,550×65 | **175,483,550×65** | +96 |
| Subtitle | 136,472,630×82 | **136,568,630×82** | +96 |
| Small | 136,573,630×37 | **136,669,630×37** | +96 |

### 7.5 店到店 Logo

-   正式 geometry：(161, 378, 580×82)。assets：
    `快速取件/assets/店到店_橘.png`、`店到店_白.png`（既有 tracked）。
-   Logo mode：auto｜orange｜white，沿 shared
    `resolveLogoVariant()`（WCAG sRGB relative luminance，threshold
    0.498708）。
-   drawing：contain（`scale = min(boxW/srcW, boxH/srcH)`，無
    min(1, …) clamp）、preserve ratio、水平＋垂直置中、no crop、
    no stretch、destination 不強制 round、high-quality smoothing。

### 7.6 Typography

-   主標：(175, 483, 550×65)、`52.5pt "ShopeeNotoSans Medium"`
    （52.5pt 原樣，不 round、不換算 px）、Workspace title color、
    ink-box 水平＋垂直置中、空字串 skip。
-   副標：(136, 568, 630×82)、`65pt "ShopeeNotoSans Bold"`、
    Workspace subtitle color、單一 font 字串 ink-box 水平＋垂直
    置中、空字串 skip。**Jamie override：所有字元（含 `$`、`%`）
    一律 65pt Bold**——FSS D－06 原為 main 65pt／$·% 55pt symbol，
    快速取件明確不採 55pt symbol、symbol 縮小、tokenizer、
    mixed-size runs 與特殊 symbol baseline。
-   小字：(136, 669, 630×37)、`30pt "ShopeeNotoSans Medium"`、
    Workspace small color、ink-box 水平＋垂直置中、空字串 skip。
-   所有 pt 保持 pt，不得人工換算 px。

### 7.7 Medium Local 2×

-   主標＋小字走 renderer-local 2× supersampling：temp canvas
    **1800 × 3200**、`scale(2, 2)`、仍以原始 900×1600 geometry
    繪製，high-quality downsample 回 900×1600。
-   `title === "" && small === ""` 時 early-return。
-   副標 65pt Bold 直接繪於正式 canvas，不走 Medium layer。

### 7.8 KV

-   source：`state.shared.kv`；正式 local box：x = 24、y = 783、
    w = 851、h = 588（global CSS (2724, 783, 851×588) − artboard
    offset X 2700／Y 0）。
-   contain、preserve ratio、水平＋垂直置中、no crop、no stretch、
    destination 不強制 round、high-quality smoothing。
-   KV null → skip、無 placeholder；decode 失敗 fail-closed。

### 7.9 右上 IG 蝦皮購物（固定 asset）

-   正式 asset：`快速取件/assets/IG 蝦皮購物.png`（intrinsic
    161 × 162 RGBA，MD5 `ee0bae0e352a06c30ab7701f450fab1f`）。
-   正式 local：x = 739、y = 119、w = 161、h = 162（global CSS
    (3439, 119, 161×162) − offset 2700／0；739+161=900 貼齊右緣）。
-   **1:1 繪製**；intrinsic 非 161×162 → fail-closed。
-   固定 renderer asset，永遠原圖：不受 Logo mode、background
    luminance、`resolveLogoVariant` 影響；不新增 Workspace field、
    無 Viewer control；不得以 `蝦皮購物_橘/白.png` 取代。
-   Alignment overlay 的近似圖形不改寫「CSS＋artboard offset＋
    intrinsic dimensions」所確立的正式 geometry。

### 7.10 CTA

**05 沒有 CTA。** 無看更多 CTA、無逛逛去 CTA、無 CTA geometry／
state。

### 7.11 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  店到店 Logo
4.  Medium text layer（主標＋小字，local 2×）
5.  Bold subtitle（副標）
6.  右上 IG 蝦皮購物（1:1）

`source-over`、`globalAlpha = 1`、無 clipping region。alignment
overlay 僅 Viewer 人工驗證，不進 renderer／export。

### 7.12 JPG Encoder

-   registry `encoderOptions`：`Object.freeze({ quality: 1.0 })` —
    quality-only（依 FSS 06_IG 現行機制）。
-   無 maxBytes、floor、binary search、steps、DPI metadata patch；
    沿現有 export.js quality-only path，export.js 零修改。
-   不得把 01（245,000）／02（145,000）的 size ladder 套到 05。

### 7.13 Viewer 人工驗證

-   05 已接入既有人工驗證模式（無第二套 UI）：verification text 為
    主標「春季加碼抽」（weighted 5）、副標「副標最多$7個字」
    （weighted 7.0，含 $ 驗證全 65pt 同尺寸）、小字
    「到貨1天內完成取貨即可參加」（weighted 12.5），limits 8/7/14
    皆合法。overlay：`../assets/對位/05_IG.png`。
-   既有 controls 直接 reuse：KV upload/remove、background、
    title/subtitle/small color、Logo auto/orange/white、alignment
    overlay、opacity。IG 蝦皮購物為固定 asset，無 Viewer control。
-   Jamie Phase E：PASS。

### 7.14 Fail-Closed

Medium／Bold font load 失敗、店到店 Logo load/decode 失敗、
IG 蝦皮購物 load/decode 失敗、IG intrinsic ≠ 161×162、KV decode
失敗、JPG encode 失敗——一律 fail-closed，不得 silent fallback。
無 55pt symbol font requirement。

### 7.15 Verification Record

-   Phase A：Repository Investigation completed。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Codex self-test completed。
-   Phase E：Jamie manual verification PASS。

## 8. 06_FB Post 正式規格

### 8.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
-   Code Commit：`b4513bba6d173759125ea5ab90f055cda9c84a0e`
    （`feat(quick-pickup): add FB Post renderer`）
-   rendererKey：`06-fb-post`
-   Renderer module：`快速取件/js/renderers/06-fb-post.js`（自含必要
    helper，不修改、不 import 01～05 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/06_FB Post.command`（100755）；Viewer：
    `快速取件/launch/viewer.html?item=06`；Alignment：
    `快速取件/assets/對位/06_FB Post.png`（1200×630 RGBA，MD5
    `f6a47478889c8892e371c9fb74727924`）。

### 8.2 FSS Reference

快速取件與 FSS 的版位編號不同：06_FB Post 的 FSS 唯讀參考來源為
**FSS BN D－07（07_FB POST，1200×630，JPG）**；快速取件 06 不等於 FSS
placement number 06。06 對位圖與 FSS D－07 四文字／Logo geometry
ΔX = 0、ΔY = 0（逐 px 相同）。下列 Jamie overrides 優先於 FSS。

### 8.3 Canvas 與背景

-   Canvas：1200 × 630。Export format：JPG。controlsProfile：
    shared-01-13。
-   無固定背景圖。整張 canvas 以 `state.shared.colors.background`
    填滿（`fillRect(0, 0, 1200, 630)`）。

### 8.4 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  店到店 Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標，直繪）
6.  左下直式蝦皮購物（1:1）

`source-over`、`globalAlpha = 1`、無 clipping／masking。06 沒有 CTA、
沒有其他 fixed asset。alignment overlay 僅 Viewer 人工驗證，不進
renderer／export。

### 8.5 店到店 Logo

-   正式 runtime assets：`快速取件/assets/店到店_橘.png`、
    `快速取件/assets/店到店_白.png`（既有 tracked）。
-   Logo box：x = 54、y = 201、w = 365、h = 52。
-   規則：contain（`scale = min(boxW/srcW, boxH/srcH)`，無 min(1, …)
    clamp）、preserve ratio、**水平貼左＋垂直置中**（destX =
    box.left、destY = box.top + (box.height − destH) / 2）、no crop、
    no stretch、destination 不強制 round、high-quality smoothing。
-   Logo mode：auto｜orange｜white，沿共通 `resolveLogoVariant()`
    （threshold 0.498708）。

### 8.6 Typography

-   主標：(54, 266, 405×49)、`39pt "ShopeeNotoSans Medium"`、
    Workspace title color／text、ink LEFT＋垂直置中、空字串 skip、
    走 Medium local 2×。
-   副標：(54, 325, 475×62)、`49pt "ShopeeNotoSans Bold"`、
    Workspace subtitle color／text、ink LEFT＋垂直置中、空字串 skip、
    **直繪於正式 canvas**（不進 Medium layer）。
-   小字：(54, 401, 475×28)、`22.5pt "ShopeeNotoSans Medium"`
    （22.5pt 原樣，不取整、不換算 px）、Workspace small color／text、
    ink LEFT＋垂直置中、空字串 skip、走 Medium local 2×。
-   所有 pt 保持 pt，不得人工換算 px。

### 8.7 $／% 正式裁決

FSS D－07 存在 $／% 特殊縮小（main 49pt／symbol 41pt＋tokenizer／
mixed-run／baseline 特例），**快速取件 06 明確不沿用**：副標所有字元
（含 `$`、`%`）一律 `49pt "ShopeeNotoSans Bold"`，renderer 無 41pt、
無 symbol tokenizer、無 mixed-run、無特殊 symbol baseline、無 $/%
shrink logic。

### 8.8 Text Ink Alignment

三組文字：`textAlign = "left"`、`textBaseline = "alphabetic"`、
actualBoundingBox ink metrics；水平 `x = box.left − inkLeft`；垂直
`y = box.top + box.height / 2 − (inkTop + inkBottom) / 2`；空字串
skip。

### 8.9 Medium Local 2×

-   只適用主標＋小字：temp canvas **2400 × 1260**、`scale(2, 2)`、
    仍以原始 1200×630 logical geometry 與原始 pt 字級（39pt／22.5pt，
    不手動 ×2、不轉 px）繪製，high-quality downsample 回 1200×630。
-   `title === "" && small === ""` 時 early-return。
-   副標 Bold 不進 Medium layer。

### 8.10 KV

-   source：`state.shared.kv`（共通 KV state）；box：x = 544、y = 99、
    w = 625、h = 432。
-   contain、preserve original ratio、水平＋垂直置中、no crop、
    no stretch、destination 不強制 round、high-quality smoothing。
-   KV null → skip、無 placeholder；decode 失敗 fail-closed。

### 8.11 左下直式蝦皮購物

-   正式 assets：`快速取件/assets/直式蝦皮購物_橘.png`（MD5
    `aaa66eb8268214f275c72d3d3ed1f87f`）、
    `快速取件/assets/直式蝦皮購物_白.png`（MD5
    `36c9b5baf61860ae0af3c47b1670b7f2`），intrinsic 皆 83 × 112 RGBA。
-   正式 destination：x = 22、y = 499、w = 83、h = 112。
-   **1:1 原尺寸繪製**（Jamie 裁決）；intrinsic gate：
    naturalWidth === 83 且 naturalHeight === 112，不符 fail-closed。
    不 contain、不 stretch、不 scale。

### 8.12 Logo ＋ 左下 Asset 同步 Variant

每次 render 只做**一次** `resolveLogoVariant()`；同一 variant 同時
決定店到店 Logo 與左下直式蝦皮購物：orange → 店到店_橘＋直式蝦皮購物_橘；
white → 店到店_白＋直式蝦皮購物_白。不建立第二套 luminance 判斷。
Logo mode（auto｜orange｜white）沿既有 shared contract。

### 8.13 JPG Encoder

-   registry `encoderOptions`：`Object.freeze({ quality: 1.0 })` —
    quality-only（依 FSS 07_FB POST 現行機制）。
-   無 maxBytes、binary search、floor、steps、DPI patch／額外 DPI
    metadata requirement；沿現有 export.js quality-only path，
    export.js 零修改。

### 8.14 Viewer 人工驗證

-   06 已接入既有人工驗證模式（無第二套 UI）：verification text 為
    主標「春季加碼抽」（weighted 5）、副標「副標$7%個字」
    （weighted 5.5，同時含 $ 與 % 以驗證全 49pt 不縮小）、小字
    「到貨1天內完成取貨即可參加」（weighted 12.5），limits 8/7/14
    皆合法。overlay：`../assets/對位/06_FB Post.png`（viewer-only，
    不進 renderer／export／Workspace／JSON）。
-   verification state 僅存 page memory。

### 8.15 Launcher

-   `快速取件/launch/06_FB Post.command`（100755）：zsh、port 4175、
    SPX root server、viewer marker readiness、正確既有 server 安全
    重用、他程序佔用 fail-closed、Google Chrome 開
    `viewer.html?item=06`、沿既有 trap／cleanup pattern。
-   Jamie 已實機 Finder 雙擊 → Chrome → 06 Viewer，PASS。

### 8.16 Fail-Closed

Medium／Bold font load 失敗、店到店 Logo load/decode 失敗、直式
蝦皮購物 load/decode 失敗、直式蝦皮購物 intrinsic ≠ 83×112、KV decode
失敗——一律 fail-closed，不 silent fallback。無 41pt symbol font
requirement。

### 8.17 Verification Record

-   Phase A：Repository Investigation completed。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Claude self-test PASS。
-   Phase E：Jamie manual verification PASS（實機 Finder 雙擊
    launcher → Chrome → 06 Viewer 人工畫面驗證通過）。

## 9. 07_TVBN_有人店 正式規格

### 9.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
-   Code Commit：`f0c72aa90b2e28a00f965be3e835116196958de9`
    （`feat(quick-pickup): add manned-store TVBN renderer`）
-   rendererKey：`07-tvbn-manned`
-   Renderer module：`快速取件/js/renderers/07-tvbn-manned.js`（自含
    必要 helper，不修改、不 import 01～06 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/07_TVBN_有人店.command`（100755）；
    Viewer：`快速取件/launch/viewer.html?item=07`；Alignment：
    `快速取件/assets/對位/07_TVBN_有人店.png`（1599×1080 RGBA，MD5
    `154083b6c42086f4ced54318d7d53d6a`）。

### 9.2 Geometry Authority 與 Artboard Offset

07 的 geometry authority 為 Jamie 提供之 Photoshop raw CSS ＋ 07
對位圖：六元素以同一組 artboard offset（X = 143、Y = 1734）唯一互證
換算為 canvas-local。FSS D－09（09_SPX TVBN_2，1599×1080）僅作
rendering pattern／encoder 唯讀 evidence，其 geometry／字級與 07 不同，
不得搬入。raw／global 座標不參與 runtime geometry。

### 9.3 Canvas 與背景

-   Canvas：1599 × 1080。Export format：JPG。controlsProfile：
    shared-01-13。
-   無固定背景圖。整張 canvas 以 `state.shared.colors.background`
    填滿（`fillRect(0, 0, 1599, 1080)`）；renderer 不 hardcode 正式
    背景色（shared default 仍為 #fee1d6，由 Workspace 控制）。

### 9.4 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  店到店 Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標，直繪）
6.  右上橫式蝦皮購物

`source-over`、`globalAlpha = 1`、無 clipping／masking。07 沒有 CTA、
沒有固定背景 asset。alignment overlay 僅 Viewer 人工驗證，不進
renderer／export。

### 9.5 正式 Geometry

| 元素 | x | y | w | h |
|---|---|---|---|---|
| Logo | 59 | 279 | 640 | 101 |
| Title | 59 | 416 | 640 | 78 |
| Subtitle | 59 | 518 | 690 | 92 |
| Small | 59 | 639 | 690 | 46 |
| KV | 788 | 273 | 772 | 534 |
| 右上蝦皮購物 | 1347 | 47 | 212 | 52 |

### 9.6 Typography（含 Photoshop source pt → Canvas pt precedent）

-   正式 Canvas renderer 字級：Title `63.75pt "ShopeeNotoSans
    Medium"`、Subtitle `73.5pt "ShopeeNotoSans Bold"`、Small
    `37.5pt "ShopeeNotoSans Medium"`。
-   **Photoshop source pt 記錄（供 08～15 後續版位沿用之
    precedent）**：Jamie 自 Photoshop 直接讀得之 source 字級為
    85pt／98pt／50pt（Photoshop 顯示之 pt，非 px）。依既有 FSS
    Photoshop-to-Canvas mapping（×0.75）換算為 Canvas renderer pt：
    85×0.75＝63.75、98×0.75＝73.5、50×0.75＝37.5。此 mapping 由
    FSS 全系列 D templates（26 組 font↔設計框高樣本）唯讀驗證確立。
    後續版位若字級來源為 Photoshop source pt，須套用同一 ×0.75
    precedent，不得直接作 Canvas pt 使用。
-   Title＋Small 走 renderer-local 2× rasterization（見 9.7）；
    Subtitle 直繪。
-   文字對位：ink metrics（actualBoundingBox*）、`textAlign =
    "left"`、`textBaseline = "alphabetic"`；水平 `x = box.left −
    inkLeft`、垂直 `y = box.top + box.height/2 − (inkTop +
    inkBottom)/2`；空字串 skip。

### 9.7 Medium Local 2×

-   僅主標＋小字：temp canvas **3198 × 2160**、`scale(2, 2)`、仍以
    原始 1599×1080 logical geometry 與 logical 字級（63.75pt／
    37.5pt，不二次放大）繪製，high-quality downsample 回 1599×1080。
    2× 只提升 raster quality，不改 logical geometry／字級。
-   `title === "" && small === ""` 時 early-return。

### 9.8 Subtitle $／% 正式裁決

Subtitle 整串（含 `$`、`%`）一律 `73.5pt "ShopeeNotoSans Bold"`；
07 不沿用 FSS D－09 的 symbol special sizing（65pt symbol／
tokenizer／mixed-run），renderer 無任何 symbol runtime path。

### 9.9 KV

-   source：`state.shared.kv`（01～13 shared KV）；box：(788, 273,
    772×534)。
-   contain、preserve aspect ratio、水平＋垂直置中、no crop、
    no stretch、destination 不強制 round、high-quality smoothing。
-   KV null → skip、無 placeholder；decode 失敗 fail-closed。

### 9.10 店到店 Logo

-   assets：`快速取件/assets/店到店_橘.png`、`店到店_白.png`
    （asset intrinsic 1401×275；renderer 無 Logo runtime intrinsic
    gate，與核准 Proposal 及 01～06 precedent 一致）。
-   box：(59, 279, 640×101)。
-   contain、preserve ratio、**靠左＋垂直置中**、no crop、
    no stretch、destination 不強制 round、high-quality smoothing、
    無 min(1, …) clamp。

### 9.11 右上橫式蝦皮購物

-   assets：`快速取件/assets/蝦皮購物_橘.png`（MD5
    `0cc5b7ce24422173dfe05bc214d78543`）、`蝦皮購物_白.png`（MD5
    `8d3af22c425c7f8a75a6abf86b53e0c8`），intrinsic 皆 1119×275
    RGBA。
-   box：(1347, 47, 212×52)。
-   contain、preserve ratio、**靠右＋垂直置中**（destX = box.left +
    box.width − destW，不 round；高度先到 → destH = 52、destW ≈
    211.585）、no crop、no stretch。
-   **runtime intrinsic gate：1119×275**，不符 fail-closed。
-   與店到店 Logo 共用**同一次** `resolveLogoVariant()` 結果同步
    切換 橘／白（每次 render 只解算一次；orange → 橘＋橘、white →
    白＋白），無第二套 luminance 判斷。

### 9.12 JPG Encoder

-   registry `encoderOptions`：`Object.freeze({ quality: 1.0 })` —
    quality-only（依 FSS 09_SPX TVBN_2 現行機制）。
-   無 maxBytes、binary search、floor、steps、DPI patch；沿現有
    export.js quality-only path，export.js 零修改。

### 9.13 Viewer 人工驗證

-   07 已接入既有人工驗證模式（無第二套 UI）：verification text 為
    主標「春季加碼抽」（weighted 5）、副標「副標$7%個字」
    （weighted 5.5，含 $ 與 % 驗證全 73.5pt 不縮小）、小字
    「到貨1天內完成取貨即可參加」（weighted 12.5）。
-   overlay：`../assets/對位/07_TVBN_有人店.png`（viewer-only，不進
    Workspace／JSON／renderer／export／localStorage）。
-   verification state 僅存 page memory。

### 9.14 Launcher

-   `快速取件/launch/07_TVBN_有人店.command`（100755）：zsh、
    port 4175、SPX root server、viewer marker readiness、正確既有
    server 安全重用、他程序佔用 fail-closed、Google Chrome 開
    `viewer.html?item=07`、沿既有 trap／cleanup pattern。

### 9.15 Verification Record

-   Phase A：Repository Investigation completed。
-   Phase B：Proposal approved。
-   Phase C：Coding completed（含 Jamie 核准之 Text Size
    Correction：Photoshop source 85/98/50pt → Canvas
    63.75/73.5/37.5pt）。
-   Phase D：AI/Claude self-test PASS。
-   Phase E：Jamie manual verification PASS（實機 Finder 雙擊
    launcher → Chrome → Viewer 驗證，含字級修正後 Re-Verification
    PASS）。

## 10. 08_TVBN_智取店 正式規格

### 10.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
    （08 lifecycle 於本節對應之 Docs Commit 完成後才 Closed）。
-   Code Commit：`ed78cd504a1ef2600b65edec03e360cb2d60c4f1`
    （`ed78cd5`，`feat(quick-pickup): add smart-store TVBN renderer`，
    parent `16ebd5c8c462daee3221ea7829590b916be4ce54`）。
-   rendererKey：`08-tvbn-smart`
-   Renderer module：`快速取件/js/renderers/08-tvbn-smart.js`（自含
    必要 helper，不修改、不 import 01～07 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/08_TVBN_智取店.command`（100755）；
    Viewer：`快速取件/launch/viewer.html?item=08`；Alignment：
    `快速取件/assets/對位/08_TVBN_智取店.png`（1080×1920 RGBA，MD5
    `cdee6dda372b40d25c87b33a135f8606`，已於上述 Code Commit 正式
    入庫；viewer-only，不是 renderer asset）。

### 10.2 Geometry Authority 與 Artboard Offset

08 的 geometry authority 為 Jamie 提供之 Photoshop source ＋ 08
對位圖：六元素以同一組 artboard offset（X = 1846、Y = 1734）唯一互證
換算為 canvas-local（Phase A 六元素交叉驗證一致）。FSS D－08
（08_SPX TVBN_1，1080×1920）僅作 rendering pattern／typography
behavior／encoder 唯讀 evidence，其 geometry／字級與 08 不同，不得
作為 08 geometry authority。raw／global 座標不參與 runtime geometry。

### 10.3 Canvas 與背景

-   Canvas：1080 × 1920。Export format：JPG。controlsProfile：
    shared-01-13。
-   無固定背景圖。整張 canvas 以 `state.shared.colors.background`
    填滿（`fillRect(0, 0, 1080, 1920)`）；renderer 不 hardcode 正式
    背景色（shared default 仍為 #fee1d6，由 Workspace 控制）。

### 10.4 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  店到店 Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標，直繪）
6.  右下橫式蝦皮購物

`source-over`、`globalAlpha = 1`、無 clipping／masking。08 沒有 CTA、
沒有固定背景 asset。alignment overlay 僅 Viewer 人工驗證，不進
renderer／export。

### 10.5 正式 Geometry

| 元素 | x | y | w | h |
|---|---|---|---|---|
| Logo | 220 | 231 | 640 | 101 |
| Title | 220 | 358 | 640 | 84 |
| Subtitle | 115 | 468 | 850 | 115 |
| Small | 115 | 611 | 850 | 55 |
| KV | 40 | 853 | 1000 | 691 |
| 右下蝦皮購物 | 828 | 1828 | 212 | 52 |

### 10.6 Typography

-   正式 Canvas renderer 字級：Title `67.5pt "ShopeeNotoSans
    Medium"`、Subtitle `92.25pt "ShopeeNotoSans Bold"`、Small
    `44.25pt "ShopeeNotoSans Medium"`。
-   Photoshop source 字級為 90pt／123pt／59pt（Photoshop UI 直接
    顯示之 pt，非 px）。依 9.6 既有正式 precedent（Photoshop source
    pt × 0.75 → Canvas renderer pt）換算：90×0.75＝67.5、
    123×0.75＝92.25、59×0.75＝44.25。
-   Title＋Small 走 renderer-local 2× rasterization（見 10.7）；
    Subtitle 直繪。
-   文字對位：三段文字（Title／Subtitle／Small）均為 ink
    center/center（沿 05 已驗證 pattern）：ink metrics
    （actualBoundingBox*）、`textAlign = "left"`、`textBaseline =
    "alphabetic"`；水平 `x = box.left + (box.width − inkWidth)/2 −
    inkLeft`、垂直 `y = box.top + box.height/2 − (inkTop +
    inkBottom)/2`；空字串 skip。此為 ink-box 置中，非單純 CSS
    center。

### 10.7 Medium Local 2×

-   僅主標＋小字：temp canvas **2160 × 3840**、`scale(2, 2)`、仍以
    原始 1080×1920 logical geometry 與 logical 字級（67.5pt／
    44.25pt，不二次放大）繪製，high-quality downsample 回
    1080×1920。2× 只提升 raster quality，不改 logical geometry／
    字級。
-   `title === "" && small === ""` 時 early-return。
-   Subtitle 不進 Medium layer，直接在正式 canvas 繪製。

### 10.8 Subtitle $／% 正式裁決

Subtitle 整串（含 `$`、`%`）一律 `92.25pt "ShopeeNotoSans Bold"`；
08 不沿用 FSS D－08 的 symbol special sizing（75pt symbol／
tokenizer／mixed-run／special baseline behavior），renderer 無任何
symbol runtime path。

### 10.9 KV

-   source：`state.shared.kv`（01～13 shared KV）；box：(40, 853,
    1000×691)。
-   contain、preserve aspect ratio、水平＋垂直置中、no crop、
    no stretch、destination 不強制 round、high-quality smoothing。
-   KV null → skip、無 placeholder；decode 失敗 fail-closed。

### 10.10 店到店 Logo

-   assets：`快速取件/assets/店到店_橘.png`、`店到店_白.png`
    （renderer 無 Logo runtime intrinsic gate，與核准 Proposal 及
    01～07 precedent 一致）。
-   box：(220, 231, 640×101)。
-   contain、preserve ratio、**水平置中＋垂直置中**、no crop、
    no stretch、destination 不強制 round、high-quality smoothing、
    無 min(1, …) clamp。

### 10.11 右下橫式蝦皮購物

-   assets：`快速取件/assets/蝦皮購物_橘.png`、`蝦皮購物_白.png`
    （intrinsic 皆 1119×275 RGBA，07 已入庫之 tracked assets）。
-   box：(828, 1828, 212×52)。
-   正式裁決：contain 縮放後**於 box 範圍內靠右＋垂直置中**
    （destX = box.left + box.width − destW，不 round；高度先到 →
    destH = 52、destW ≈ 211.585）、preserve ratio、no crop、
    no stretch、無 min(1, …) clamp。
-   **runtime intrinsic gate：1119×275**，不符 fail-closed。
-   與店到店 Logo 共用**同一次** `resolveLogoVariant()` 結果同步
    切換 橘／白（每次 render 只解算一次；orange → 橘＋橘、white →
    白＋白），完全沿用 shared auto／orange／white，無第二套
    threshold／luminance／mode。

### 10.12 JPG Encoder

-   registry `encoderOptions`：`Object.freeze({ quality: 1.0 })` —
    quality-only（依 FSS 08_SPX TVBN_1 現行機制）。
-   無 maxBytes、quality search、floor、DPI rewrite、PNG fallback；
    沿現有 export.js quality-only path，export.js 零修改。

### 10.13 Viewer 人工驗證

-   08 已接入既有人工驗證模式（無第二套 UI）：verification text 為
    主標「春季加碼抽」（weighted 5）、副標「副標$7%個字」
    （weighted 5.5，含 $ 與 % 驗證全 92.25pt 不縮小）、小字
    「到貨1天內完成取貨即可參加」（weighted 12.5）。
-   overlay：`../assets/對位/08_TVBN_智取店.png`（viewer-only，不進
    Workspace／JSON／renderer／export／localStorage）。
-   verification state 僅存 page memory。

### 10.14 Launcher

-   `快速取件/launch/08_TVBN_智取店.command`（100755）：zsh、
    port 4175、SPX root server、viewer marker readiness、正確既有
    server 安全重用、他程序佔用 fail-closed、Google Chrome 開
    `viewer.html?item=08`（URL-encoded 路徑）、沿既有 trap／cleanup
    pattern。

### 10.15 Shared Impact 與 Readiness

-   Shared Impact：None。08 renderer self-contained；未修改
    Workspace、Editor、banwords、Excel Import、JSON、KV shared
    state、colors、Logo mode、Reset、Preview contract、export.js、
    vendor、SPX 第一層。
-   Code Commit 後：01～08 renderer＋encoder ready；09～15 仍
    unready（`getUnreadyExportItems()` = 09～15），完整 15 項
    Export 仍 fail-closed。08 ready 不代表整個 generator 可完整
    export。

### 10.16 Verification Record

-   Phase A：Repository Investigation completed（六元素 artboard
    offset X=1846／Y=1734 交叉驗證）。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Claude self-test PASS。
-   Phase E：Jamie manual verification PASS（實機 Finder 雙擊
    launcher → Chrome → Viewer 驗證：正常 Preview、alignment
    overlay、三段文字視覺、$/% 同字級、Logo 置中、KV contain、
    右下蝦皮購物 box 內靠右、Logo＋右下 asset variant 同步）。

## 11. 09_TVBN_旗艦店 正式規格

### 11.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
    （09 lifecycle 於本節對應之 Docs Commit 完成後才 Closed）。
-   Code Commit：`827ce9a1a0d717b2ca0d9e245c7d121c80c019c1`
    （`827ce9a`，`feat(quick-pickup): add flagship TVBN renderer`，
    parent `0335f531aaac891071f270b172493d47cf0c1bee`）。
-   rendererKey：`09-tvbn-flagship`
-   Renderer module：`快速取件/js/renderers/09-tvbn-flagship.js`（自含
    必要 helper，不修改、不 import 01～08 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/09_TVBN_旗艦店.command`（100755）；
    Viewer：`快速取件/launch/viewer.html?item=09`；Alignment：
    `快速取件/assets/對位/09_TVBN_旗艦店.png`（1920×1080 RGBA，
    10,805 bytes，MD5 `1fdbdbf044de340cb318dbc13930dd82`，SHA-256
    `e29020c99173e6b5cc1850ca0b7195a0b5b7e4e16afb141ba8ef8b6f25996567`，
    已於上述 Code Commit 正式入庫；viewer-only，不是 renderer asset）。

### 11.2 Geometry Authority 與 Artboard Offset

09 的 geometry authority 為 Jamie 提供之 Photoshop source ＋ 09
對位圖：六元素以同一組 artboard offset（X = 3030、Y = 1734）唯一互證
換算為 canvas-local（Phase A 六元素交叉驗證，w×h 全部精確吻合）。
FSS 全系列不存在 1920×1080 template；FSS D－09（09_SPX TVBN_2，
1599×1080）僅作 rendering pattern／typography behavior／encoder
唯讀 evidence，不得作 09 geometry authority。global source geometry
僅為 provenance 記錄；runtime renderer 直接使用 11.3 之 canvas-local
geometry，不執行 global→local runtime conversion。

### 11.3 Canvas、背景與正式 Geometry

-   Canvas：1920 × 1080。Export format：JPG。controlsProfile：
    shared-01-13。
-   無固定背景圖。整張 canvas 以 `state.shared.colors.background`
    填滿（`fillRect(0, 0, 1920, 1080)`）；renderer 不 hardcode 正式
    背景色（shared default 仍為 #fee1d6，由 Workspace 控制）。

| 元素 | x | y | w | h |
|---|---|---|---|---|
| Logo | 114 | 241 | 640 | 101 |
| Title | 114 | 368 | 640 | 82 |
| Subtitle | 114 | 478 | 830 | 116 |
| Small | 114 | 625 | 830 | 46 |
| KV | 993 | 236 | 880 | 609 |
| 右上蝦皮購物 | 1651 | 47 | 212 | 52 |

### 11.4 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  店到店 Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標，直繪）
6.  右上橫式蝦皮購物

`source-over`、`globalAlpha = 1`、無 clipping／masking。09 沒有 CTA、
沒有固定背景 asset。alignment overlay 僅 Viewer 人工驗證，不進
renderer／export。

### 11.5 Typography

-   正式 Canvas renderer 字級：Title `67.5pt "ShopeeNotoSans
    Medium"`、Subtitle `92.25pt "ShopeeNotoSans Bold"`、Small
    `37.5pt "ShopeeNotoSans Medium"`。
-   Photoshop source 字級為 90pt／123pt／50pt（Photoshop UI 直接
    顯示之 pt，非 px）。依 9.6 既有正式 precedent（Photoshop source
    pt × 0.75 → Canvas renderer pt）換算：90×0.75＝67.5、
    123×0.75＝92.25、50×0.75＝37.5。
-   Title＋Small 走 renderer-local 2× rasterization（見 11.6）；
    Subtitle 直繪。
-   文字對位：三段文字（Title／Subtitle／Small）均為**靠左＋垂直
    ink 置中**（沿 07 已驗證 left-centered ink semantics）：ink
    metrics（actualBoundingBox*；inkLeft = −actualBoundingBoxLeft、
    inkTop = −actualBoundingBoxAscent、inkBottom =
    actualBoundingBoxDescent）、`textAlign = "left"`、`textBaseline
    = "alphabetic"`；水平 `x = box.left − inkLeft`、垂直 `y =
    box.top + box.height/2 − (inkTop + inkBottom)/2`；空字串 skip。

### 11.6 Medium Local 2×

-   僅主標＋小字：temp canvas **3840 × 2160**、`scale(2, 2)`、仍以
    原始 1920×1080 logical geometry 與 logical 字級（67.5pt／
    37.5pt，不二次放大）繪製，high-quality downsample 回
    1920×1080。2× 只提升 raster quality，不改 logical geometry／
    字級。
-   `title === "" && small === ""` 時 early-return。
-   Subtitle 不進 Medium layer，直接在正式 canvas 繪製。

### 11.7 Subtitle $／% 正式裁決

Subtitle 整串（含 `$`、`%`）一律 `92.25pt "ShopeeNotoSans Bold"`；
09 不沿用 FSS D－09 的 symbol special sizing（65pt symbol font／
tokenizer／mixed-run／special baseline path），renderer 無任何
symbol runtime path。

### 11.8 KV

-   source：`state.shared.kv`（01～13 shared KV）；box：(993, 236,
    880×609)。
-   contain、preserve aspect ratio、水平＋垂直置中、no crop、
    no stretch、destination 不強制 round、無 min(1, …) clamp、
    high-quality smoothing。
-   KV null → skip、無 placeholder；decode 失敗 fail-closed；沿
    既有 dataUrl single-entry safe cache semantics。

### 11.9 店到店 Logo

-   assets：`快速取件/assets/店到店_橘.png`、`店到店_白.png`
    （asset intrinsic 1401×275；renderer 無 Logo runtime intrinsic
    gate，與核准 Proposal 及 01～08 precedent 一致）。
-   box：(114, 241, 640×101)。
-   contain、preserve ratio、**靠左＋垂直置中**（destinationX =
    box.left）、no crop、no stretch、destination 不強制 round、
    無 min(1, …) clamp、high-quality smoothing。
-   contain 為高度先到：scale = min(640/1401, 101/275) ≈ 0.36727，
    destination ≈ 514.5×101；Logo 右側留白為正式預期行為。

### 11.10 右上橫式蝦皮購物

-   assets：`快速取件/assets/蝦皮購物_橘.png`、`蝦皮購物_白.png`
    （intrinsic 皆 1119×275 RGBA，07 已入庫之 tracked assets）。
-   box：(1651, 47, 212×52)。
-   正式裁決：contain 縮放後**於 box 範圍內靠右＋垂直置中**
    （destX = box.left + box.width − destW，不 round；高度先到 →
    destH = 52、destW ≈ 211.593）、preserve ratio、no crop、
    no stretch、無 min(1, …) clamp。
-   **runtime intrinsic gate：1119×275**，不符 fail-closed。
-   與店到店 Logo 共用**同一次** `resolveLogoVariant()` 結果同步
    切換 橘／白（每次 render 只解算一次；orange → 橘＋橘、white →
    白＋白），完全沿用 shared auto／orange／white，無第二套
    brightness／threshold／mode。

### 11.11 Font Gate

-   僅使用 SPX root 正式字型
    `fonts/ShopeeNotoSans(content)-Medium.woff2`／`-Bold.woff2`
    （renderer relative URL `../../../fonts/`，無新增字型副本）。
-   FontFace → `load()` → `document.fonts.add` →
    `document.fonts.load` → `document.fonts.check`；任一必要 font
    不可用即 throw fail-closed，不 fallback 後繼續正式 render。

### 11.12 JPG Encoder

-   registry `encoderOptions`：`Object.freeze({ quality: 1.0 })` —
    quality-only（依 FSS 09_SPX TVBN_2 現行機制）。
-   無 maxBytes、qualityFloor、searchSteps、DPI rewrite、PNG
    fallback、UPNG；沿現有 export.js quality-only path，export.js
    零修改。

### 11.13 Viewer 人工驗證

-   09 已接入既有人工驗證模式（無第二套 UI）：verification text 為
    主標「春季加碼抽」（weighted 5）、副標「副標$7%個字」
    （weighted 5.5，含 $ 與 % 驗證全 92.25pt 不縮小）、小字
    「到貨1天內完成取貨即可參加」（weighted 12.5）。
-   overlay：`../assets/對位/09_TVBN_旗艦店.png`（viewer-only，不進
    Workspace／JSON／renderer／export／localStorage／
    sessionStorage）。
-   verification state 僅存 page memory。

### 11.14 Launcher

-   `快速取件/launch/09_TVBN_旗艦店.command`（100755）：zsh、
    port 4175、SPX root server、viewer marker readiness、正確既有
    server 安全重用、他程序佔用 fail-closed、Google Chrome 開
    `viewer.html?item=09`（URL-encoded 路徑）、沿既有 trap／cleanup
    pattern。

### 11.15 Shared Impact 與 Readiness

-   Shared Impact：None。09 renderer self-contained；未修改
    Workspace、Editor、banwords、Excel Import、JSON、KV shared
    state contract、Color control、Logo mode、Preview／Renderer
    contract、Export readiness gate、vendor、SPX 第一層。
-   Code Commit 後：01～09 renderer＋encoder ready；10～15 仍
    unready（`getUnreadyExportItems()` = 10～15），完整 15 項
    Export 仍 fail-closed。09 ready 不代表整個 generator 可完整
    export。10 對位圖已由 Jamie 本人主動刪除（已授權操作），不
    改變 10 的 Deferred／Unready 狀態。

### 11.16 Verification Record

-   Phase A：Repository Investigation completed（六元素 artboard
    offset X=3030／Y=1734 交叉驗證；09 對位圖為 alpha 層級實心
    矩形佔位框）。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Claude self-test PASS。
-   Phase E：Jamie manual verification PASS（實機 Finder 雙擊
    launcher → Chrome → Viewer 驗證）。

## 12. 10_繳費機直式BN-立保 正式規格（reuse 08 renderer）

### 12.1 Status 與 Code Commit

-   Status：Implemented（reuse 08 renderer）/ Jamie Manual
    Verification PASS（10 lifecycle 於本節對應之 Docs Commit 完成後
    才 Closed）。
-   Code Commit：`894337559172fa1a9a175ea517393180f23e1b42`
    （`8943375`，`feat(quick-pickup): add payment-machine vertical BN
    reuse`，parent `cfd0e23ae537a2062c01ad8eb9025eef36465fb0`）。
-   rendererKey：`08-tvbn-smart`（與 08 共用）。
-   **10 不存在獨立 renderer implementation**（無 `10-*.js`）。
    Launcher：`快速取件/launch/10_繳費機直式BN-立保.command`
    （100755）；Viewer：`快速取件/launch/viewer.html?item=10`。

### 12.2 Renderer Reuse（正式 architecture decision）

-   Jamie 裁決：08_TVBN_智取店 與 10_繳費機直式BN-立保 被正式定義為
    **同一 runtime layout contract**（1080×1920、同構圖）。兩個
    registry entries 共同指向 `08-tvbn-smart`；renderer
    implementation 只有一份（`快速取件/js/renderers/08-tvbn-smart.js`，
    本輪零修改），此為 **intentional coupling**。
-   Runtime identity evidence：08 renderer 不依賴 item.id／
    item.name／placement identity（render function 僅使用 ctx 與
    state）；Canvas backing size 由 10 自身 registry entry（1080×
    1920）提供；Export 檔名／placement identity 仍由 10 registry
    entry 驅動。
-   **Future-maintenance rule**：未來修改 `08-tvbn-smart` 會同步
    影響 08 與 10 輸出——這是預期行為。只有未來正式需求使 08／10
    runtime layout contract 出現差異時，才拆出 10-specific
    renderer；不得預先複製。

### 12.3 正式 Contract（由共用 08-tvbn-smart renderer 提供）

-   Canvas：1080 × 1920。Format：JPG。controlsProfile：
    shared-01-13。encoderOptions：`Object.freeze({ quality: 1.0 })`
    — quality-only（無 maxBytes／qualityFloor／searchSteps／DPI／
    PNG fallback）。
-   以下 geometry／行為**不是新的 10 implementation constants**，
    均由共用 renderer contract 提供（詳見 §10）：

| 元素 | x | y | w | h |
|---|---|---|---|---|
| Logo | 220 | 231 | 640 | 101 |
| Title | 220 | 358 | 640 | 84 |
| Subtitle | 115 | 468 | 850 | 115 |
| Small | 115 | 611 | 850 | 55 |
| KV | 40 | 853 | 1000 | 691 |
| 右下蝦皮購物 | 828 | 1828 | 212 | 52 |

-   Typography：Photoshop source 90pt／123pt／59pt（Photoshop UI
    顯示之 pt，非 px）依既有 mapping ×0.75 → Canvas `67.5pt
    Medium`／`92.25pt Bold`／`44.25pt Medium`。
-   Text：三段文字 ink center/center（actualBoundingBox）；Title＋
    Small 走 renderer-local 2×（temp 2160×3840、scale(2,2)、
    logical geometry／字級不二次放大）；Subtitle 直繪；$／% 不縮小
    （整串 92.25pt Bold）。
-   Background：Workspace shared background color（無固定底圖）。
-   KV：`state.shared.kv`、contain、center/center、preserve
    ratio、no crop／stretch／rounding、null skip、decode
    fail-closed、safe dataUrl cache。
-   Logo：店到店_橘/白、contain、center/center、無 runtime
    intrinsic gate。右下蝦皮購物：蝦皮購物_橘/白、contain、
    RIGHT＋vCenter、runtime intrinsic gate 1119×275。每 render
    單次 `resolveLogoVariant()`，同一 variant 同步控制 Logo＋
    Shopee。
-   Draw order：background → KV → Logo → Medium（Title＋Small）→
    Subtitle 直繪 → 右下蝦皮購物。Fonts：SPX root Medium／Bold
    WOFF2、既有 fail-closed gate。

### 12.4 Viewer 人工驗證（alignment reuse）

-   verification text：主標「春季加碼抽」、副標「副標$7%個字」、
    小字「到貨1天內完成取貨即可參加」。
-   10 **不建立自己的 alignment PNG**；
    `assets/對位/10_繳費機直式BN-立保.png` 不存在（Jamie 本人主動
    刪除，已授權），且不需要建立。10 的 Viewer overlay 刻意 reuse
    `assets/對位/08_TVBN_智取店.png`（08／10 採同一 geometry
    contract）。
-   alignment 僅為 viewer-only verification aid，不是 renderer／
    export／readiness dependency。verification state 僅存 page
    memory。

### 12.5 Launcher

-   `快速取件/launch/10_繳費機直式BN-立保.command`（100755）：沿
    既有 locked launcher pattern（zsh、port 4175、SPX root
    server、viewer marker readiness、safe reuse、他程序佔用
    fail-closed、Google Chrome 開 `viewer.html?item=10`、trap／
    cleanup）。

### 12.6 Shared Impact 與 Readiness

-   Shared Impact：None。本輪僅 registry 10 接線、viewer 10
    verification 接線與 10 launcher（2 M＋1 A）；renderers/
    index.js、08-tvbn-smart.js、export.js 與所有 shared 零修改。
-   Code Commit 後：01～10 renderer＋encoder ready；11～15 仍
    unready（`getUnreadyExportItems()` = 11～15），完整 15 項
    Export 仍 fail-closed。11～13 對位圖的存在不代表該版位
    readiness。

### 12.7 Verification Record

-   Phase A：Renderer Reuse Investigation PASS（08 runtime
    contract 建檔、identity independence 與 reuse 機制實證）。
-   Phase B：Reuse Proposal approved。
-   Phase C：Coding PASS（2 M＋1 A 接線）。
-   Phase D：AI/Claude self-test PASS。
-   Phase E：Jamie manual verification PASS（item=10 正式
    renderer 正常顯示、08 alignment reuse 正常、人工視覺驗證
    PASS）。

## 13. 11_繳費機直式BN-博辰 正式規格

### 13.1 Status 與 Code Commit

-   Status：Implemented / Jamie Manual Verification PASS
    （11 lifecycle 於本節對應之 Docs Commit 完成後才 Closed）。
-   Code Commit：`03c1c4f828e9836baba56cd460bb6940b8377ac2`
    （`03c1c4f`，`feat(quick-pickup): add Bochen payment-machine
    vertical BN renderer`，parent
    `a6d4c53006ce1a42cc5f2db34491415a5d4dc73e`）。
-   rendererKey：`11-payment-vertical-bochen`
-   Renderer module：
    `快速取件/js/renderers/11-payment-vertical-bochen.js`（自含必要
    helper，不修改、不 import 01～10 renderer），經
    `快速取件/js/renderers/index.js` 以 `registerRenderer()` 註冊。
-   Launcher：`快速取件/launch/11_繳費機直式BN-博辰.command`
    （100755）；Viewer：`快速取件/launch/viewer.html?item=11`；
    Alignment：`快速取件/assets/對位/11_繳費機直式BN-博辰.png`
    （2700×3380 RGBA，43,733 bytes，MD5
    `f8540d6e8275fc1a955eb7115406aff5`，已於上述 Code Commit 正式
    入庫；viewer-only，不是 renderer asset）。

### 13.2 Geometry Authority 與 Artboard Offset

11 的 geometry authority 為 Jamie 提供之 Photoshop source ＋ 11
對位圖：五個未爭議元素以同一組 artboard offset（X = 1213、
Y = 4096）唯一互證換算為 canvas-local。Subtitle 的正式 geometry
由對位圖確立：Jamie 原提供之 Subtitle CSS 與 Title 重複（貼錯），
而對位圖恰有六個實心矩形，五個由未爭議元素認領後，剩餘唯一矩形
即 Subtitle——local (350,866,2000×259)、對應 global
(1563,4962,2000×259)。此為 alignment geometry evidence，不是
runtime conversion；renderer 使用 local constants，runtime 不套
global offset。

### 13.3 Canvas、背景與正式 Geometry

-   Canvas：2700 × 3380。Export format：JPG。controlsProfile：
    shared-01-13。
-   無固定背景圖。整張 canvas 以 `state.shared.colors.background`
    填滿（`fillRect(0, 0, 2700, 3380)`）。

| 元素 | x | y | w | h |
|---|---|---|---|---|
| Logo | 600 | 323 | 1500 | 219 |
| Title | 600 | 609 | 1500 | 184 |
| Subtitle | 350 | 866 | 2000 | 259 |
| Small | 350 | 1213 | 2000 | 117 |
| KV | 100 | 1389 | 2500 | 1730 |
| 左下蝦皮購物 | 104 | 3169 | 529 | 130 |

### 13.4 Draw Order

1.  Workspace background（整張填色）
2.  KV（`state.shared.kv` 存在時）
3.  店到店 Logo
4.  Medium text layer（主標＋小字，local 2× supersampling）
5.  Bold subtitle（副標，直繪）
6.  左下橫式蝦皮購物

`source-over`、`globalAlpha = 1`、無 clipping／masking。11 沒有
CTA、沒有固定背景 asset。alignment overlay 僅 Viewer 人工驗證，
不進 renderer／export。

### 13.5 Typography

-   正式 Canvas renderer 字級：Title `150pt "ShopeeNotoSans
    Medium"`、Subtitle `206.25pt "ShopeeNotoSans Bold"`、Small
    `93.75pt "ShopeeNotoSans Medium"`。
-   Photoshop source 字級為 200pt／275pt／125pt（Photoshop UI
    直接顯示之 pt，非 px）。依 9.6 既有正式 precedent（Photoshop
    source pt × 0.75 → Canvas renderer pt）換算：200×0.75＝150、
    275×0.75＝206.25、125×0.75＝93.75。
-   文字對位：三段文字均為 ink center/center（actualBoundingBox
    Left／Right／Ascent／Descent 量測；`textAlign = "left"`、
    `textBaseline = "alphabetic"`）；空字串 skip。

### 13.6 Medium Local 2×

-   僅主標＋小字：temp canvas **5400 × 6760**、`scale(2, 2)`、仍以
    原始 2700×3380 logical geometry 與 logical 字級（150pt／
    93.75pt，不二次放大）繪製，high-quality downsample 回
    2700×3380。
-   `title === "" && small === ""` 時 early-return。
-   Subtitle 不進 Medium layer，直接在正式 canvas 繪製。

### 13.7 Subtitle $／% 正式裁決

Subtitle 整串（含 `$`、`%`）一律 `206.25pt "ShopeeNotoSans
Bold"`；renderer 無任何 symbol special sizing／tokenizer／
mixed-run runtime path。

### 13.8 KV

-   source：`state.shared.kv`（01～13 shared KV）；box：(100,
    1389, 2500×1730)。
-   contain、center/center、preserve ratio、no crop、no stretch、
    destination 不強制 round、high-quality smoothing。
-   KV null → skip、無 placeholder；decode 失敗 fail-closed；沿
    既有 safe dataUrl single-entry cache。

### 13.9 店到店 Logo

-   assets：`快速取件/assets/店到店_橘.png`、`店到店_白.png`
    （asset intrinsic 1401×275；renderer 無 Logo runtime
    intrinsic gate）。
-   box：(600, 323, 1500×219)。
-   contain、**center/center**、preserve ratio（高度先到，
    destination ≈ 1115.7×219）。

### 13.10 左下橫式蝦皮購物

-   assets：`快速取件/assets/蝦皮購物_橘.png`、`蝦皮購物_白.png`
    （intrinsic 皆 1119×275 RGBA）。
-   box：(104, 3169, 529×130)。
-   正式裁決：contain 縮放後**於 box 範圍內靠左＋垂直置中**
    （destX = box.left，不 round；高度先到 → destH = 130、
    destW ≈ 528.98）、preserve ratio。
-   **runtime intrinsic gate：1119×275**，不符 fail-closed。

### 13.11 Logo Variant

每次 render 只呼叫一次既有 `resolveLogoVariant()`，同一 resolved
variant 同步控制店到店 Logo ＋ 左下蝦皮購物（orange → 橘＋橘、
white → 白＋白），完全沿用 shared auto／orange／white，無第二套
背景判斷。

### 13.12 Font／Failure Contract

-   SPX root `fonts/ShopeeNotoSans(content)-Medium.woff2`／
    `-Bold.woff2`；FontFace → `load()` → `document.fonts.add` →
    `document.fonts.load` → `document.fonts.check`，任一失敗
    throw fail-closed，不 fallback。
-   Logo／Shopee static asset promise cache；KV safe dataUrl
    cache；必要 asset decode failure 一律 fail-closed。

### 13.13 JPG Encoder

-   registry `encoderOptions`：`Object.freeze({ quality: 1.0 })`
    — quality-only；無 maxBytes／qualityFloor／searchSteps／DPI
    ／PNG fallback；export.js 零修改。

### 13.14 Viewer 人工驗證

-   verification text：主標「春季加碼抽」、副標「副標$7%個字」
    （含 $ 與 % 驗證全 206.25pt 不縮小）、小字
    「到貨1天內完成取貨即可參加」。
-   overlay：`../assets/對位/11_繳費機直式BN-博辰.png`
    （viewer-only，不進 Workspace／JSON／renderer／export／
    readiness）。verification state 僅存 page memory。

### 13.15 Launcher

-   `快速取件/launch/11_繳費機直式BN-博辰.command`（100755）：沿
    10 locked launcher pattern（port 4175、viewer item=11），僅
    placement substitution；已於 Jamie Phase E 實機啟動並完成
    驗證。

### 13.16 Shared Impact 與 Readiness

-   Shared Impact：None。11 renderer self-contained；未修改任何
    shared platform；11 的 2700×3380／Medium 2× backing／LB
    Shopee LEFT／Logo center 等均為 11 placement contract，不
    泛化為 shared 規則。
-   Code Commit 後：01～11 renderer＋encoder ready；12～15 仍
    unready（`getUnreadyExportItems()` = 12～15），完整 15 項
    Export 仍 fail-closed。12～13 對位圖的存在不代表該版位
    readiness。

### 13.17 Verification Record

-   Phase A：Repository Investigation completed（五元素 offset
    X=1213／Y=4096 交叉驗證；Subtitle 由對位圖第六實心矩形
    確立）。
-   Phase B：Proposal approved。
-   Phase C：Coding completed。
-   Phase D：AI/Claude self-test PASS。
-   Phase E：Jamie manual verification PASS（實機 Finder 雙擊
    launcher → Chrome → Viewer 驗證）。

## 14. 12_繳費機下方BN-立保

-   Name：`12_繳費機下方BN-立保`；Size：1040×578；Format：JPG。
-   Status：Deferred / Not Implemented。
-   逐版位正式 renderer／geometry／asset／encoder 規格待該版位完成並經
    Jamie PASS 後補入本文件。

## 15. 13_繳費機下方BN-博辰

-   Name：`13_繳費機下方BN-博辰`；Size：984×309；Format：JPG。
-   Status：Deferred / Not Implemented。
-   逐版位正式 renderer／geometry／asset／encoder 規格待該版位完成並經
    Jamie PASS 後補入本文件。

## 16. 14_AR

-   Name：`14_AR`；Size：100×100；Format：JPG。
-   Status：Deferred / Not Implemented。
-   14 的右側欄與特殊需求（含 Excel-managed data）須於實際製作時重新
    確認；逐版位正式規格待該版位完成並經 Jamie PASS 後補入本文件。

## 17. 15_MSBN

-   Name：`15_MSBN`；Size：1200×400；Format：JPG。
-   Status：Deferred / Not Implemented。
-   15 的右側欄與特殊需求（含 Excel-managed data）須於實際製作時重新
    確認；逐版位正式規格待該版位完成並經 Jamie PASS 後補入本文件。
